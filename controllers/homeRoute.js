const router = require('express').Router();
// add model const
// add auth require path

// router.get
// this will get and render the homepage from handlebars
// this will also handle the login and redirect if not logged in. 